export type Product = {
    id: string;
    name: string;
    price: number;
    description: string;
}

export type CreateProduct = {
    id: string;
    name: string;
    price: number;
    description: string;
}